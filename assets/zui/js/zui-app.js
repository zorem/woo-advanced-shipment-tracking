/*!
 * Zorem UI — zui-app.js
 *
 * Plugin-specific UI behaviour belongs in each plugin's own JS bundle.
 * This file is intentionally empty: plugin-specific UI code belongs
 * in each plugin's own JS bundle, not in this shared library.
 */